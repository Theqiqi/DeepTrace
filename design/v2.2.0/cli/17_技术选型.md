# CLI - 技术选型(修改模式,v2.2.0)

> **沿用** v2.0.0/cli/17_技术选型.md:程序类型=命令行可执行文件(exe);C++20;
> Windows;Console;CMake(CMakeLists.txt + CMakePresets.json,Ninja + MSVC);vcpkg
> (manifest 含 gtest);参数解析=getopt 风格/argc+argv(自实现);依赖=静态库 deeptrace
> (find_library 引用 ../DeepTrace/out/lib/<配置>,显式链接 keystone/capstone)。

本版本无新增依赖、无技术选型变化。
