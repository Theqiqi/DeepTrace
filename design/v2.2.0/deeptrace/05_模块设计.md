# deeptrace - 模块设计(修改模式,增量,v2.2.0)

> 引用:v2.0.0/deeptrace/05_模块设计.md 既有模块结构。本版本新增功能落在既有模块:

## 1. 改动点 → 模块

| 改动点 | 层/模块 | 文件 |
|--------|---------|------|
| shellcode_alloc / run / free 公共 API | Service 层 inject 模块(组装:记录查找/更新 + 基础设施调用) | src/service/inject.h/cpp |
| 公共 API 声明 | 对外头文件 | include/deeptrace.h |

## 2. 未改动模块

Domain(types.h,InjectInfo 复用)、Algorithm、Infrastructure(inject/memory 能力已有,
仅被 Service 复用)、utils(log)均不新增内容,引用既有文档。
