# deeptrace - 代码规范(修改模式,v2.12.0)

> 沿用:既有代码规范。v2.12.0 增量约束:

- 线程池仅标准库头(<thread>/<mutex>/<condition_variable>/<queue>);
  禁止 windows.h 线程 API(CreateThread 等,远程线程除外)。
- Algorithm 层函数纯计算:输入内存 buffer + 参数,输出结果;禁止 I/O。
- 公共头结构体成员用标准 C++ 类型。
- 注释用英文,与既有风格一致。
