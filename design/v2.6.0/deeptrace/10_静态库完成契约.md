# deeptrace - 静态库完成契约(v2.6.0)

## 1. 完成项

| 项 | 内容 |
|----|------|
| 公共头文件 | DeepTrace/include/deeptrace.h(含 `script_symbol` 声明) |
| API 列表 | `Result script_symbol(const std::string& name, uintptr_t* out_addr)` |
| 库产物 | Debug/Release 编译通过(out/lib/<配置>/deeptrace.lib) |
| 能力覆盖 | 按名查脚本符号记录地址,支持 Ok/NotFound/NotAttached/InvalidArg |
| 验证证据 | 集成测试(真实 target):alloc 后查到同一地址;free 后 NotFound;非法名 InvalidArg |

## 2. 回交 CLI

- CLI 接口层 `resolve_addr` 调用 `deeptrace::script_symbol`(通过公共头,
  不包含静态库内部头)。
- 覆盖检查:CLI「符号寻址」能力与 `script_symbol` 一一对应,匹配完成。
