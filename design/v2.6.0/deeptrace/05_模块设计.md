# deeptrace - 模块设计(v2.6.0,增量)

> 引用:v2.3.0/deeptrace 文档基线。v2.6.0 改动仅落在 service/script 模块。

| 层 | 文件 | 改动 |
|----|------|------|
| 公共头 | include/deeptrace.h | script 段新增 `script_symbol` 声明 |
| Service | src/service/script.h | 新增 `script_symbol` 声明 |
| Service | src/service/script.cpp | 实现(复用 valid_symbol_name + load_script_symbols) |
| 测试 | test/integration/process_integration_test.cpp | script_symbol 用例 |

其余模块(store/session/asm/hook/thread)不变,记录格式与存储路径不动。
