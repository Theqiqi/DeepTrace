# CLI - 模块设计(修改模式,增量,v2.8.0)

> 引用:v2.7.0/cli/10_模块设计.md。v2.8.0 **无生产代码改动**;仅测试层新增
> mem write <symbol> 用例 + 版本号。

## 1. 改动点 → 层 / 文件映射

| 改动点 | 层 | 文件 | 说明 |
|--------|----|------|------|
| mem write 符号形状单测 | 测试 | cli/test/unit/parser_test.cpp | parse({mem,write,<symbol>,...}) 通过 |
| mem write 符号写入读回集成 | 测试 | cli/test/integration/cli_integration_test.cpp | aptr fixture + hex/dec 读回 |
| e2e 动态改指针目标 | 测试 | cli/test/e2e/test_cli_e2e.py | mem write <符号> 后 mem read 新值 |
| 用例清单 | 文档 | cli/test/e2e/cases.md | v2.8.0 用例表 |
| 版本号 2.8.0 | 打印/命令层 | printer.cpp、commands.cpp | help/version 字符串 |

## 2. 职责边界(无改动)

- 生产代码(cmd_memory.cpp resolve_addr 链路)零改动,维持 v2.6.0 边界:
  解析层只校验形状,接口层 attach 后查符号,打印层输出不变。
