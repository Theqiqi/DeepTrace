# CLI - 代码审查记录(修改模式,v2.1.0 debug 单一入口)

> 按 `.flow/cli_无交互_新建与修改程序流程.md` §6 逐项审查受影响文件。

## 1. 审查范围

- 修改:`command/commands.cpp`(debug 组仅保留 run + 帮助文本 v2.1.0)、
  `command/parser.cpp`(删除失效 hw-type 校验)、`command/commands.h`(参数类型注释同步)、
  `interface/cmd_debug.cpp`(仅转发 run)、`printing/printer.cpp`(v2.1.0)、
  `cli/CMakeLists.txt`(v2.1.0)
- 不变:`interface/script.cpp`(op 表即调试能力全量入口)、`interface/cmd_debug_run.cpp`
- 测试:`test/unit/parser_test.cpp`(DebugSingleCommandsRejected 负例)、
  `test/integration/cli_integration_test.cpp`(删除单命令用例,新增拒绝负例 + 会话清理回归)、
  `test/e2e/test_cli_e2e.py` + `cases.md`(15 个单命令拒绝 + 目标无损 + debug run 回归)

## 2. 逐项审查结果

### 2.1 目录结构(§6.1)

- 无新目录/嵌套变化;新 fixture `cli/test/scripts/debug_break_only.json` 随 POST_BUILD
  部署,与既有 fixture 同模式。通过。

### 2.2 层级与无交互(§6.2)

- cmd_debug 仅转发 run 到 cmd_debug_run;无新增平台 API/第三方库;无等待输入。
- 删除的 15 个单命令在 parser 层被拦截(unknown command,exit 2),不会到达
  cmd_debug;cmd_debug 中 InvalidArg 分支为防御性兜底。通过。

### 2.3 API 使用(§6.3)

- 脚本 op 表全量覆盖静态库调试 API(break/clear/hbreak/hclear/guard/unguard/
  pause/resume/step/next/registers/register/status/continue),无遗漏;
  原单命令能力 100% 可由脚本表达(12_接口设计 §3 对照)。通过。

### 2.4 模块职责(§6.4)

- parser 拒绝未知 debug 动作;cmd_debug 仅转发;cmd_debug_run 持会话生命周期;
  printer 只格式化。职责清晰。通过。

### 2.5 代码质量(§6.5)

- 退出码:未知 debug 动作 → 2;脚本错误 → 2;会话/步骤失败 → 1;成功 → 0。
- 无残留 0xCC 路径:单命令被拒绝(不执行),脚本会话结束 cleanup 恢复字节
  (新增 DebugRunSessionCleanupRestoresByte 回归验证)。通过。

## 3. 审查发现与修复

| # | 级别 | 问题 | 修复 |
|---|------|------|------|
| 1 | 轻微 | parser.cpp 的 `valid_hw_type`/`hw-type` 分派因 `debug hbreak` 删除而失效(死代码);commands.h 注释仍列 hw-type | 删除 parser 死代码,注释同步(已修复) |
| 2 | 测试缺口 | 会话结束清理路径(断点未清除即 detach)在 CLI 层无测试:既有 fixture 均在会话内 clear,detach 时字节恢复未被断言 | 新增 fixture `debug_break_only.json`(只 break 不 clear)+ 集成用例断言字节恢复 0x44 且目标存活(已修复) |

## 4. 结论

修复后重新编译并全量运行:单元 99、集成 24、e2e 104 全部通过。审查通过。
