# deeptrace - 模块设计(v2.7.0,增量)

> 引用:v2.6.0/deeptrace 文档基线。v2.7.0 改动落在 infrastructure/memory 与
> service/script 两个模块。

| 层 | 文件 | 改动 |
|----|------|------|
| 基础设施 | src/infrastructure/memory/memory.h | 新增 `RemoteAllocNear` 声明 |
| 基础设施 | src/infrastructure/memory/memory.cpp | 实现(两遍 VirtualQueryEx 窗口扫描) |
| 公共头 | include/deeptrace.h | script 段新增 `script_alloc_near` 声明 |
| Service | src/service/script.h | 新增 `script_alloc_near` 声明 |
| Service | src/service/script.cpp | 实现(校验/记录/落盘/回滚复用 script_alloc 路径) |
| 测试 | test/integration/process_integration_test.cpp | script_alloc_near 用例 |

其余模块(store/session/asm/hook/thread)不变,记录格式与存储路径不动。
