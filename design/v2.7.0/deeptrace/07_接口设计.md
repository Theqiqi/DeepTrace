# deeptrace - 接口设计(v2.7.0,增量)

## 1. 公共 API 声明

```cpp
// include/deeptrace.h, script 段(在 script_alloc 之后)
Result script_alloc_near(const std::string& name, size_t size, uintptr_t anchor,
                         const std::string& owner, uintptr_t* out_addr);
```

## 2. 基础设施(memory.h/cpp)

```cpp
// memory.h
Result RemoteAllocNear(void* hprocess, size_t size, uint32_t protection,
                       uintptr_t anchor, uintptr_t* out_addr);
```

实现要点(两遍扫描,见 03_需求分析 §3.1):

```cpp
Result RemoteAllocNear(void* hprocess, size_t size, uint32_t protection,
                       uintptr_t anchor, uintptr_t* out_addr) {
    const uintptr_t kDisp = 0x7FFFFFFF;
    const uintptr_t high = (anchor > UINTPTR_MAX - kDisp) ? UINTPTR_MAX : anchor + kDisp;
    const uintptr_t low  = (anchor < kDisp) ? 0 : anchor - kDisp;

    auto try_alloc = [&](uintptr_t cand) -> uintptr_t {
        if (cand == 0 || cand > high || cand < low) return 0;
        if (cand + size < cand) return 0;  // overflow
        if (cand + size - 1 > high) return 0;
        void* p = ::VirtualAllocEx(static_cast<HANDLE>(hprocess),
                                   reinterpret_cast<LPVOID>(cand), size,
                                   MEM_COMMIT | MEM_RESERVE, protection);
        return reinterpret_cast<uintptr_t>(p);
    };
    auto align_up = [](uintptr_t v) { return (v + 0xFFFF) & ~uintptr_t(0xFFFF); };
    auto align_dn = [](uintptr_t v) { return v & ~uintptr_t(0xFFFF); };

    // Upward pass: nearest free region at/above the anchor.
    uintptr_t pos = anchor;
    while (pos <= high) {
        MEMORY_BASIC_INFORMATION mbi;
        if (!::VirtualQueryEx(static_cast<HANDLE>(hprocess),
                              reinterpret_cast<LPCVOID>(pos), &mbi, sizeof mbi))
            break;
        uintptr_t base = reinterpret_cast<uintptr_t>(mbi.BaseAddress);
        uintptr_t end  = base + mbi.RegionSize;
        if (mbi.State == MEM_FREE) {
            uintptr_t cand = align_up(base > anchor ? base : anchor);
            if (cand >= base && cand + size <= end) {
                uintptr_t p = try_alloc(cand);
                if (p) { *out_addr = p; return Result::Ok; }
            }
            if (end > high) break;  // window ceiling reached
        }
        if (end <= pos) break;  // no progress guard
        pos = end;
    }

    // Downward pass: nearest free region below the anchor.
    pos = anchor;
    while (pos >= low) {
        MEMORY_BASIC_INFORMATION mbi;
        if (!::VirtualQueryEx(static_cast<HANDLE>(hprocess),
                              reinterpret_cast<LPCVOID>(pos), &mbi, sizeof mbi))
            break;
        uintptr_t base = reinterpret_cast<uintptr_t>(mbi.BaseAddress);
        uintptr_t end  = base + mbi.RegionSize;
        if (mbi.State == MEM_FREE) {
            // Usable portion of this free region within the window below the
            // anchor; try the highest aligned position (closest to anchor).
            uintptr_t top  = (end < anchor) ? end : anchor;
            uintptr_t cand = align_dn(top - size);
            uintptr_t floor = base > low ? base : low;
            if (cand >= floor && cand + size <= top && cand + size - 1 >= low) {
                uintptr_t p = try_alloc(cand);
                if (p) { *out_addr = p; return Result::Ok; }
            }
        }
        if (base == 0) break;
        pos = base - 1;  // next region strictly below
    }
    return Result::Error;
}
```

- MEM_FREE 区域 RegionSize 即整个空闲区;向上优先(含锚点所在区 → 距锚点
  最近可能位置),再向下。
- 失败(窗口内无空闲 / VirtualAllocEx 拒绝)→ Error,绝不回退任意选址。

## 3. Service(script.cpp)

```cpp
Result script_alloc_near(const std::string& name, size_t size, uintptr_t anchor,
                         const std::string& owner, uintptr_t* out_addr) {
    auto& s = internal::session();
    if (!s.handle) return Result::NotAttached;
    if (!valid_symbol_name(name) || size == 0 || !out_addr) return Result::InvalidArg;

    auto recs = internal::load_script_symbols(s.pid);
    if (symbol_exists(recs, name)) return Result::InvalidArg;  // duplicate

    uintptr_t remote = 0;
    Result r = internal::RemoteAllocNear(s.handle, size, PAGE_EXECUTE_READWRITE,
                                         anchor, &remote);
    if (r != Result::Ok) return r;

    recs.push_back(internal::ScriptSymbolRecord{name, remote, owner});
    if (!internal::save_script_symbols(s.pid, recs)) {
        internal::RemoteFree(s.handle, remote);
        return Result::Error;
    }
    *out_addr = remote;
    return Result::Ok;
}
```

- 记录/落盘/回滚与 script_alloc 逐行一致,仅分配路径不同。
- 就近失败返回 Error(与 script_alloc 分配失败语义一致),绝不回退任意选址。

## 4. 消费方

CLI `exec_enable` Phase 1:有 near 锚点调本 API,无锚点调 `script_alloc`;
锚点来自 CLI `resolve_enable` 解析出的 near_anchors 表。
