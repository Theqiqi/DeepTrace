# deeptrace - 静态库完成契约(v2.7.0)

## 1. 完成项

| 项 | 内容 |
|----|------|
| 公共头文件 | DeepTrace/include/deeptrace.h(含 `script_alloc_near` 声明) |
| API 列表 | `Result script_alloc_near(name, size, anchor, owner, out_addr)` |
| 库产物 | Debug/Release 编译通过(out/lib/<配置>/deeptrace.lib) |
| 能力覆盖 | 按锚点就近分配(±2GB 窗口、贴近锚点) + 符号记录/落盘/回滚(与 script_alloc 一致);就近失败 Error,不静默回退 |
| 验证证据 | 集成测试(真实 target):near alloc 落点与锚点距离 < 2GB 且贴近;script_symbol 可查到同一地址;script_free 后 NotFound;非法参数 InvalidArg |

## 2. 回交 CLI

- CLI `exec_enable` Phase 1 分派:有 near 锚点(来自 resolve_enable 的
  near_anchors 表)调 `deeptrace::script_alloc_near`,无锚点调 `script_alloc`
  (通过公共头,不包含静态库内部头)。
- 覆盖检查:CLI「alloc near 真实就近分配」能力与 `script_alloc_near` +
  `RemoteAllocNear` 一一对应,匹配完成。
