# deeptrace - 代码规范(修改模式)

> 沿用:v1.2.0/deeptrace/12_代码规范.md 既有规范。落实点:

- 公共类型仅标准 C++ 类型(ContinueInfo 不含 windows.h 类型);
- Infrastructure 不引用 service/store.h;头文件包含风格 <库名> 式无相对路径;
- 新增代码保持既有命名/风格(匿名命名空间辅助、提前返回、单职责);
- include/domain/types.h 与 src/domain/types.h 保持同步(新增 ContinueInfo 两处同加)。
