# CLI - 接口调用设计(修改模式)

> 输入:09_能力匹配分析(含静态库完成契约回填)、静态库 API 列表。
> 静态库完成契约(design/v2.0.0/deeptrace/10_静态库完成契约.md)已返回,
> 缺失能力 `debug_continue` 已确认覆盖。

## 1. 步骤 → 静态库公共 API 映射

| 脚本 op | 静态库 API | 说明 |
|---------|-----------|------|
| break | `breakpoint_set(addr, &bp)` | 记录 orig 用于清理 |
| clear | `breakpoint_clear(addr)` | |
| hbreak | `hw_breakpoint_set(addr, type, length)` | |
| hclear | `hw_breakpoint_clear(addr)` | |
| guard | `guard_set(addr, size)` | 记录区域用于清理 |
| unguard | `guard_clear(addr, size)` | |
| pause / resume | `debug_pause()` / `debug_resume()` | |
| step / next | `debug_step(tid,&rip)` / `debug_step_over(tid,&rip)` | |
| registers | `registers_get(regs, tid)` | |
| register | `register_get(name,&v,tid)` | |
| status | `debug_status(st)` | |
| continue | `debug_continue(timeout_ms, info)`(新,v2.0.0) | hit/exited/timeout 分支见下 |
| read | `memory_read(addr, buf, size, &n)` | format=hex → 字节;text → ASCII |
| write | `memory_write(addr, bytes, n, &n)` | |
| disasm | `disasm_at(addr, count, insns)` | |
| watch_add | `watch_add(desc, addr, type)` | |
| watch_remove | `watch_remove(index)` | |
| watch_clear | `watch_clear()` | |
| watch_refresh | `watch_refresh(entries)` | |
| watch_list | `watch_list(entries)` | |

## 2. continue 结果分支

- `Ok && hit && exception==0x80000003(EXCEPTION_BREAKPOINT)` → `breakpoint hit at <addr> (rip = <rip>)`
- `Ok && hit && 其他` → `exception 0x<code> at <addr>`
- `Ok && exited` → `process exited (code N)` → 停止脚本,退出 1
- `Ok && 超时`(DebugWaitEvent 返回 Timeout 由服务层转 Ok+全零?否——见库契约
  「未解决问题」:服务层超时返回 Result::Timeout)→ `continue timeout (<ms> ms)` → 步骤成功
- `NotAttached/InvalidArg/Error` → 步骤失败路径

## 3. -p 自动 attach/detach 路径覆盖

- `-p <pid>` 由 executor 统一处理:attach → 执行命令 → detach(既有路径)。
- `debug run` 在其中:attach 后先 `debug_attach()`(进入调试模式),脚本执行,
  清理本次断点/守护页后 `debug_detach()`,再走 executor 统一 detach。
- 成对资源:attach/detach、debug_attach/debug_detach 在成功/失败路径均配对
  (见 07 功能点 2.1 与 15 异常设计 §3);配对正确性由测试阶段「副作用检查」验证
  (目标进程在会话后仍存活)。

## 4. 检查项

- 每个 pmem/静态库调用对应真实公共 API:是(上表逐条映射)。
- 匹配已完成且完成契约已返回:是。
- 新命令覆盖 -p 自动 attach/detach 路径:是。
