# CLI - 命令列表(修改模式)

> 引用:v1.0.0/cli/13_命令列表.md 既有命令表。本版本:debug 组新增 run 动作;
> 同步登记 commands.cpp 命令表与帮助文本。

## 1. debug 组(新增 run)

```text
debug run <script.json>        Run a scripted debug session (one invocation = one session)
```

- 用法:`deeptrace_cli -p <pid> debug run <script.json>`
- 脚本 = JSON 步骤数组;步骤覆盖调试/内存/反汇编/监视:
  `break/clear/hbreak/hclear/guard/unguard/pause/resume/step/next/registers/register/
  status/continue/read/write/disasm/watch_add/watch_remove/watch_clear/watch_refresh/
  watch_list`
- 一次调用 = 一次完整调试会话:attach → debug_attach → 逐条执行 → 清理 → detach,
  会话状态全程在内存中,调用结束即结束。
- 需要 `-p <pid>`(executor 的 -p 自动 attach 路径覆盖本命令)。

### 使用示例

```jsonc
// demo.json
[
  {"op": "break", "addr": "0x140001000"},
  {"op": "continue", "timeout_ms": "5000"},
  {"op": "registers"},
  {"op": "step"},
  {"op": "read", "addr": "0x140001000", "size": "16", "format": "hex"},
  {"op": "watch_add", "desc": "g_int", "addr": "0x140001000", "type": "dword"},
  {"op": "watch_list"},
  {"op": "clear", "addr": "0x140001000"}
]
```

```text
deeptrace_cli -p 1234 debug run demo.json
```

## 2. 其余命令组

ps/mem/module/thread/debug(既有动作)/disasm/watch/dll/asm/shellcode/convert 全部
命令不变,引用既有文档。`debug run` 不修改任何既有命令行为。
