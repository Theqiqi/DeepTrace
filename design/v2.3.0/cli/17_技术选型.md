# CLI - 技术选型(修改模式,增量,v2.3.0)

> 引用:v2.2.0/cli/17_技术选型.md 既有选型。本版本沿用,无变化:

- 程序类型:命令行可执行文件(沿用);语言:C++20;平台:Windows;框架:Console。
- 构建:CMake(CMakeLists.txt + CMakePresets.json);包管理:vcpkg。
- 参数解析:argc/argv + 既有 parser(沿用)。
- 脚本解析:自实现(标准库文本解析,不引入第三方)。
- 汇编:静态库 deeptrace 的 asm_assemble_labels(内部 Keystone)。
