# CLI - 代码规范(修改模式,增量,v2.3.0)

> 引用:v2.2.0/cli/18_代码规范.md 既有规范。本版本沿用,新增约束:

- CLI 源码不包含静态库内部头文件;只经 `deeptrace.h` 公共 API 交互。
- CLI 生产代码禁止使用平台相关 API 或第三方库,只允许静态库公共 API 与 C/C++ 标准库
  (脚本解析为纯标准库文本处理)。
- 所有输出字符串纯 ASCII 可打印字符。
- 脚本解析器与执行器放 interface 层(cmd_script.cpp),遵循既有 cmd_* 风格。
- 不引入阻塞输入等待;执行完立即退出。
