# CLI - 代码规范(修改模式)

> 沿用:v1.0.0/cli/18_代码规范.md 既有规范,本版本不修改规范本身,仅标注落实点。

- CLI 源码不包含静态库内部头文件;仅通过 `deeptrace.h` 公共 API 交互 ✓
- 生产代码禁止平台相关 API 与第三方库;convert 转换仅用 C/C++ 标准库 ✓
- 所有输出字符串纯 ASCII 可打印字符 ✓
- 分层单向调用;新增代码保持既有风格(匿名命名空间辅助函数、internal 命名空间)✓
- 新增文件 cmd_convert.cpp 与既有 cmd_*.cpp 同风格;错误消息统一 `Error:` 前缀 ✓
