# CLI - 程序类型判断(修改模式)

> 沿用:程序类型不变,仍为 CLI 无交互形式。不做重复判断。

- 新增的 `convert` 命令完全符合 CLI 无交互判定标准:
  - 操作可抽象为命令+参数:`convert <type> <value>`
  - 输入可抽象为参数:类型与值均为位置参数
  - 输出可文本化:单行十六进制字节
  - 一次执行一个操作:一次调用只转换一个值,执行完立即退出
- 程序类型判断结论:**沿用既有 CLI 无交互类型**,无变更。
